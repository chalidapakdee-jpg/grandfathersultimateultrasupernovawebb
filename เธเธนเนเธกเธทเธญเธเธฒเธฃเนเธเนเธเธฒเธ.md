# คู่มือ: Grand Father's Ultimate Ultra Supernova Webb

## 1. วิธีเปิดใช้งานในเครื่องของคุณ (Local)

### สิ่งที่ต้องมีก่อน
- ติดตั้ง **Node.js เวอร์ชัน 18 ขึ้นไป** จาก https://nodejs.org (เลือกเวอร์ชัน LTS)
- มีกล้องเว็บแคมหรือกล้องมือถือ (สำหรับทดสอบเมนู "เกมการเคลื่อนไหว")

### ขั้นตอน
1. แตกไฟล์ `grandfathers-supernova.zip` ที่ได้รับ ไปยังโฟลเดอร์ที่ต้องการ
2. เปิดโปรแกรม Terminal (บน Mac/Linux) หรือ Command Prompt / PowerShell (บน Windows)
3. เข้าไปในโฟลเดอร์โปรเจกต์ด้วยคำสั่ง:
   ```
   cd path/to/grandfathers-supernova
   ```
4. ติดตั้งไลบรารีที่จำเป็นทั้งหมดด้วยคำสั่ง:
   ```
   npm install
   ```
   (ขั้นตอนนี้ต้องเชื่อมต่ออินเทอร์เน็ต และอาจใช้เวลา 1–3 นาที)
5. รันเซิร์ฟเวอร์สำหรับพัฒนา (development server) ด้วยคำสั่ง:
   ```
   npm run dev
   ```
6. เปิดเบราว์เซอร์ (แนะนำ Chrome หรือ Edge) แล้วไปที่ที่อยู่:
   ```
   http://localhost:3000
   ```
7. เมื่อเข้าเมนู "เกมการเคลื่อนไหว" เบราว์เซอร์จะขอสิทธิ์ใช้กล้อง กด **อนุญาต**
   (การใช้งานผ่าน `localhost` สามารถใช้กล้องได้โดยไม่ต้องมี HTTPS)
8. หากต้องการหยุดเซิร์ฟเวอร์ ให้กด `Ctrl + C` ในหน้าต่าง Terminal

> หมายเหตุ: ข้อมูลผู้ใช้ทั้งหมด (บัญชี คะแนน ประวัติ โปรไฟล์) ถูกเก็บไว้ใน
> เบราว์เซอร์ของเครื่องนั้น ๆ เท่านั้น (`localStorage`) ยังไม่มีการซิงค์ข้าม
> อุปกรณ์ในเวอร์ชันนี้ เพราะเป็นแอปแบบ front-end ล้วน ไม่มีเซิร์ฟเวอร์ฐานข้อมูล
> (ดูหัวข้อ "ข้อจำกัด" ด้านล่าง)

---

## 2. วิธี Deploy ขึ้นเซิร์ฟเวอร์แบบฟรี

แอปนี้สร้างด้วย Next.js จึงแนะนำให้ deploy บน **Vercel** (ผู้พัฒนา Next.js เอง)
เพราะรองรับ Next.js เต็มรูปแบบ มี HTTPS ให้ฟรีทันที (จำเป็นสำหรับกล้องบน
โดเมนจริงที่ไม่ใช่ localhost) และมีแพ็กเกจฟรีสำหรับโปรเจกต์ส่วนตัว

### วิธีที่ 1: ผ่านเว็บไซต์ Vercel + GitHub (แนะนำ ทำครั้งเดียวใช้ได้ตลอด)
1. สร้างบัญชี GitHub ฟรีที่ https://github.com (ถ้ายังไม่มี)
2. สร้าง repository ใหม่ แล้วอัปโหลดโค้ดในโฟลเดอร์ `grandfathers-supernova`
   ขึ้นไป (ลาก-วางไฟล์ผ่านหน้าเว็บ GitHub ได้ หรือใช้คำสั่ง git ก็ได้)
3. ไปที่ https://vercel.com แล้วสมัครสมาชิกด้วยบัญชี GitHub (ฟรี)
4. กด **Add New... → Project**
5. เลือก repository ที่เพิ่งอัปโหลดไป แล้วกด **Import**
6. Vercel จะตรวจพบว่าเป็นโปรเจกต์ Next.js โดยอัตโนมัติ ไม่ต้องตั้งค่าอะไรเพิ่ม
   กด **Deploy** ได้เลย
7. รอประมาณ 1–2 นาที จะได้ลิงก์เว็บไซต์ เช่น
   `https://grandfathers-supernova.vercel.app` ที่เปิดได้ทั้งบนคอมพิวเตอร์
   และมือถือ (iOS/Android) ผ่านเบราว์เซอร์ทันที
8. ทุกครั้งที่แก้ไขโค้ดแล้วอัปขึ้น GitHub ใหม่ Vercel จะ deploy เวอร์ชันใหม่
   ให้อัตโนมัติ

### วิธีที่ 2: ผ่านคำสั่ง Vercel CLI (สำหรับผู้ที่ถนัด Terminal)
1. ติดตั้งเครื่องมือ Vercel:
   ```
   npm install -g vercel
   ```
2. ในโฟลเดอร์โปรเจกต์ พิมพ์คำสั่ง:
   ```
   vercel
   ```
3. ทำตามคำแนะนำบนหน้าจอ (ล็อกอิน/สมัครบัญชีฟรี, ยืนยันชื่อโปรเจกต์)
   ระบบจะ deploy และแสดงลิงก์เว็บไซต์ให้ทันที
4. เมื่อพร้อมใช้งานจริง ให้พิมพ์คำสั่ง `vercel --prod` เพื่อ deploy เป็น
   เวอร์ชันหลัก (production)

### ทางเลือกฟรีอื่น ๆ
ทางเลือกอื่นที่รองรับ Next.js และมีแพ็กเกจฟรีเช่นกัน ได้แก่ **Netlify**
(https://netlify.com) และ **Cloudflare Pages** (https://pages.cloudflare.com)
ขั้นตอนคล้ายกัน คือเชื่อมกับ GitHub แล้วให้ระบบ build/deploy ให้อัตโนมัติ

---

## ข้อจำกัดที่ควรทราบ (เพราะเป็นแอป Front-end ล้วน)
- **บัญชีผู้ใช้ยังไม่ซิงค์ข้ามอุปกรณ์** — ข้อมูลเก็บในเบราว์เซอร์เครื่องนั้น ๆ
  หากต้องการให้ผู้สูงอายุล็อกอินคนเดียวกันได้ทั้งมือถือและคอมพิวเตอร์แล้วเห็น
  ข้อมูลตรงกัน จำเป็นต้องเพิ่ม **เซิร์ฟเวอร์และฐานข้อมูลจริง** (เช่น
  Node.js + PostgreSQL หรือบริการอย่าง Supabase/Firebase) เพื่อเก็บบัญชีและ
  คะแนนไว้ส่วนกลาง — เป็นขั้นตอนถัดไปที่แนะนำหากจะนำไปใช้งานจริง
- **รหัสผ่านยังไม่ได้เข้ารหัส** เหมาะสำหรับทดลองใช้งานเท่านั้น หากขึ้นระบบจริง
  ต้องมีเซิร์ฟเวอร์เข้ารหัสรหัสผ่านก่อนเก็บเสมอ
